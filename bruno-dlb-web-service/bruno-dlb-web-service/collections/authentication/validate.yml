info:
  name: validate
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:8089/dlb-web-service/v1/auth/validate
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
